- name: RHEL Update Security Script
  hosts: all
  tasks:
    - name: Update Message
      debug:
        msg: "Updating the Red Hat Server to the latest patches and security updates!"
    - name: Ec2 to Root Mode
      shell: sudo su
      ignore_errors: true
    - name: Update the Server
      shell: yum update -y
      ignore_errors: true